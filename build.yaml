targets:
  $default:
    builders:
      freezed:
        generate_for:
          include:
            - lib/firestore_sample/model/*.dart
      json_serializable:
        generate_for:
          include:
            - lib/firestore_sample/model/*.dart
        options:
          any_map: true
          explicit_to_json: true